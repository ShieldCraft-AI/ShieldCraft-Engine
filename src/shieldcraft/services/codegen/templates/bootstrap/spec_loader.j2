# Bootstrap Spec Loader
# Source: {{source_pointer}}
# Section: {{source_section}}
# Item: {{item_id}}

{{block:metadata}}
"""
Spec Loader for ShieldCraft Engine
Generated from: {{source_pointer}}
"""
{{endblock}}

import json
from pathlib import Path


class SpecLoader:
    """Load and validate spec files."""
    
    def load(self, path):
        """{{text}}"""
        with open(path, 'r') as f:
            return json.load(f)
    
    def validate_structure(self, spec):
        """Validate spec has required fields."""
        required = ['metadata', 'model', 'sections']
        for field in required:
            if field not in spec:
                raise ValueError(f"Missing required field: {field}")
        return True
