# Rule Template
# Item: {{item_id}}
# Ptr: {{ptr}}

class Rule:
    """{{text}}"""
    
    def evaluate(self, context):
        # Rule evaluation placeholder (deterministic stub)
        return True
