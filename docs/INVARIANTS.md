## Required invariants

This document defines why invariants are required for safe instruction processing.

- Invariants enumerate the properties that must hold for operations to be considered safe.
- Invariants are used by the engine to reason about safe modifications and test coverage.
