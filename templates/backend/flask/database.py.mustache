from flask_sqlalchemy import SQLAlchemy

# Don't create db instance here - it will be created in the app factory
db = None

def init_db(app):
    """Initialize the database with the Flask app"""
    global db
    if db is None:
        db = SQLAlchemy(app)
    return db