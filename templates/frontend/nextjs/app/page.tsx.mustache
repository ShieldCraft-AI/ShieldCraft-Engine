import Link from 'next/link'

export default function Home() {
  return (
    <div className="text-center">
      <h1 className="text-4xl font-bold text-gray-900 mb-8">Welcome to {{app_title}}</h1>
      <p className="text-xl text-gray-600 mb-12">{{app_description}}</p>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-{{entities_count}} gap-6 max-w-4xl mx-auto">
        {{#entities}}
        <div className="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition-shadow">
          <h2 className="text-xl font-semibold text-gray-900 mb-4">{{name}} Management</h2>
          <p className="text-gray-600 mb-4">Manage {{name_lower}} records in your application.</p>
          <Link
            href="/{{name_lower}}"
            className="inline-flex items-center px-4 py-2 bg-primary-600 text-white rounded-md hover:bg-primary-700 transition-colors"
          >
            View {{name}}s
          </Link>
        </div>
        {{/entities}}
      </div>
    </div>
  )
}