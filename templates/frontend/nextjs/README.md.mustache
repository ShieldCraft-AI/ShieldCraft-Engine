# {{app_title}}

{{app_description}}

A Next.js application generated from ShieldCraft Engine specifications.

## Features

- **Next.js 14** with App Router
- **TypeScript** for type safety
- **Tailwind CSS** for styling
- **Axios** for API communication
- **Responsive design** with mobile-first approach

## Getting Started

### Prerequisites

- Node.js 18+
- npm or yarn

### Installation

1. Install dependencies:
```bash
npm install
```

2. Copy environment variables:
```bash
cp .env.local.example .env.local
```

3. Update the API URL in `.env.local`:
```
NEXT_PUBLIC_API_URL=http://localhost:8000
```

### Development

Start the development server:
```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.

### Build

Build for production:
```bash
npm run build
npm start
```

## Project Structure

```
├── app/                 # Next.js app directory
│   ├── layout.tsx      # Root layout
│   ├── page.tsx        # Home page
│   └── globals.css     # Global styles
├── components/         # Reusable components
│   ├── EntityList.tsx  # List component for entities
│   └── EntityForm.tsx  # Form component for entities
├── lib/                # Utility libraries
│   └── api.ts         # API client
├── types/              # TypeScript type definitions
│   └── index.ts       # Generated types
└── public/            # Static assets
```

## API Integration

This frontend expects a backend API running at the URL specified in `NEXT_PUBLIC_API_URL`. The API should provide the following endpoints for each entity:

- `GET /{{entity_name_lower}}` - List entities
- `POST /{{entity_name_lower}}` - Create entity
- `GET /{{entity_name_lower}}/:id` - Get entity by ID
- `PUT /{{entity_name_lower}}/:id` - Update entity
- `DELETE /{{entity_name_lower}}/:id` - Delete entity

## Generated Entities

{{#entities}}
- **{{name}}**: {{description}}
{{/entities}}

## Development

### Type Checking

```bash
npm run type-check
```

### Linting

```bash
npm run lint
```

## Deployment

This application can be deployed to Vercel, Netlify, or any platform that supports Next.js applications.

For Vercel deployment:
1. Push your code to GitHub
2. Connect your repository to Vercel
3. Set the `NEXT_PUBLIC_API_URL` environment variable
4. Deploy

## License

Generated by ShieldCraft Engine