/** @type {import('next').NextConfig} */
const nextConfig = {
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: '{{api_base_url}}/api/:path*',
      },
    ]
  },
}

module.exports = nextConfig